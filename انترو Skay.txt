local TweenService = game:GetService("TweenService")
local RunService   = game:GetService("RunService")
local CoreGui      = game:GetService("CoreGui")
local Players      = game:GetService("Players")

local old = CoreGui:FindFirstChild("SKAY_INTRO")
if old then old:Destroy() end

local camera       = workspace.CurrentCamera
local viewportSize = camera.ViewportSize
local UIScale      = viewportSize.Y / 450

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name           = "SKAY_INTRO"
ScreenGui.ResetOnSpawn   = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Global
ScreenGui.Parent         = CoreGui
Instance.new("UIScale", ScreenGui).Scale = UIScale

local SolidBlackBG = Instance.new("Frame")
SolidBlackBG.Size                   = UDim2.new(1, 0, 1, 0)
SolidBlackBG.BackgroundColor3       = Color3.fromRGB(0, 0, 0)
SolidBlackBG.BackgroundTransparency = 0
SolidBlackBG.BorderSizePixel        = 0
SolidBlackBG.ZIndex                 = 0
SolidBlackBG.Parent                 = ScreenGui

local BlackBG = Instance.new("Frame")
BlackBG.Size                   = UDim2.new(1, 0, 1, 0)
BlackBG.BackgroundColor3       = Color3.fromRGB(0, 0, 0)
BlackBG.BackgroundTransparency = 0.5
BlackBG.BorderSizePixel        = 0
BlackBG.ZIndex                 = 1
BlackBG.Parent                 = ScreenGui

local BgImageLabel = Instance.new("ImageLabel")
BgImageLabel.Size                   = UDim2.new(1, 0, 1, 0)
BgImageLabel.BackgroundTransparency = 1
BgImageLabel.Image                  = "rbxassetid://136275689820128"
BgImageLabel.ScaleType              = Enum.ScaleType.Crop
BgImageLabel.ImageTransparency      = 1
BgImageLabel.ZIndex                 = 2
BgImageLabel.Parent                 = BlackBG

local DarkOverlay = Instance.new("Frame")
DarkOverlay.Size                   = UDim2.new(1, 0, 1, 0)
DarkOverlay.BackgroundColor3       = Color3.fromRGB(0, 0, 0)
DarkOverlay.BackgroundTransparency = 1
DarkOverlay.BorderSizePixel        = 0
DarkOverlay.ZIndex                 = 3
DarkOverlay.Parent                 = BlackBG

local StarImage = Instance.new("ImageLabel")
StarImage.Size                   = UDim2.fromOffset(155, 155)
StarImage.Position               = UDim2.new(0.5, 0, 0.5, 0)
StarImage.AnchorPoint            = Vector2.new(0.5, 0.5)
StarImage.BackgroundTransparency = 1
StarImage.Image                  = "rbxassetid://135791544469522"
StarImage.ScaleType              = Enum.ScaleType.Fit
StarImage.ImageTransparency      = 1
StarImage.ZIndex                 = 4
StarImage.Parent                 = BlackBG

local starAngle = 0
RunService.Heartbeat:Connect(function(dt)
    if not StarImage or not StarImage.Parent then return end
    starAngle = (starAngle + 55 * dt) % 360
    StarImage.Rotation = starAngle
end)

local BlurEffect = Instance.new("BlurEffect")
BlurEffect.Size   = 0
BlurEffect.Parent = camera

local function SafePlaySound(id, vol)
    task.spawn(function()
        local sound = Instance.new("Sound")
        sound.SoundId = id
        sound.Volume  = vol or 0.5
        sound.Parent  = ScreenGui
        sound:Play()
        task.delay(5, function()
            if sound and sound.Parent then sound:Destroy() end
        end)
    end)
end

SafePlaySound("rbxassetid://72989805559135", 0.5)

local rotatingBorders = {}
local function AddRotatingBorder(target, thickness, speed)
    if target:FindFirstChildOfClass("UIStroke") then return end
    thickness = thickness or 1.5
    speed     = speed or 90

    local stroke = Instance.new("UIStroke")
    stroke.Thickness       = thickness
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    stroke.Color           = Color3.new(1, 1, 1)
    stroke.Parent          = target

    local grad = Instance.new("UIGradient")
    grad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0,    Color3.fromRGB(255, 255, 255)),
        ColorSequenceKeypoint.new(0.45, Color3.fromRGB(160, 160, 160)),
        ColorSequenceKeypoint.new(1,    Color3.fromRGB(30,  30,  30)),
    })
    grad.Parent = stroke

    table.insert(rotatingBorders, {grad = grad, speed = speed})
    return stroke
end

RunService.Heartbeat:Connect(function(dt)
    for _, b in ipairs(rotatingBorders) do
        if b.grad and b.grad.Parent then
            b.grad.Rotation = (b.grad.Rotation + b.speed * dt) % 360
        end
    end
end)

local MainScreen = Instance.new("Frame")
MainScreen.Size                   = UDim2.new(1, 0, 1, 0)
MainScreen.BackgroundTransparency = 1
MainScreen.ZIndex                 = 5
MainScreen.Parent                 = BlackBG

local LogoFrame = Instance.new("Frame")
LogoFrame.Size                   = UDim2.fromOffset(640, 130)
LogoFrame.Position               = UDim2.new(0.5, -320, 0.08, 0)
LogoFrame.BackgroundTransparency = 1
LogoFrame.ZIndex                 = 5
LogoFrame.Parent                 = MainScreen

local LogoTitle = Instance.new("TextLabel")
LogoTitle.Text             = "VXS HUB"
LogoTitle.Font             = Enum.Font.SourceSansBold
LogoTitle.TextSize         = 68
LogoTitle.TextColor3       = Color3.fromRGB(255, 255, 255)
LogoTitle.BackgroundTransparency = 1
LogoTitle.Size             = UDim2.fromOffset(550, 85)
LogoTitle.Position         = UDim2.fromOffset(0, 0)
LogoTitle.TextXAlignment   = Enum.TextXAlignment.Left
LogoTitle.TextTransparency = 1
LogoTitle.RichText         = true
LogoTitle.ZIndex           = 6
LogoTitle.Parent           = LogoFrame

local titleStroke = Instance.new("UIStroke", LogoTitle)
titleStroke.Color           = Color3.fromRGB(80, 80, 80)
titleStroke.Thickness       = 2.5
titleStroke.Transparency    = 0.3
titleStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Contextual

local titleGrad = Instance.new("UIGradient", LogoTitle)
titleGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0,    Color3.fromRGB(255, 255, 255)),
    ColorSequenceKeypoint.new(0.55, Color3.fromRGB(190, 190, 190)),
    ColorSequenceKeypoint.new(1,    Color3.fromRGB(80,  80,  80)),
})
titleGrad.Rotation = 95

local SubLabel = Instance.new("TextLabel")
SubLabel.Text             = "WELCOME || VxS"
SubLabel.Font             = Enum.Font.SourceSansBold
SubLabel.TextSize         = 20
SubLabel.TextColor3       = Color3.fromRGB(200, 200, 200)
SubLabel.BackgroundTransparency = 1
SubLabel.Size             = UDim2.fromOffset(300, 28)
SubLabel.Position         = UDim2.fromOffset(2, 88)
SubLabel.TextXAlignment   = Enum.TextXAlignment.Left
SubLabel.TextTransparency = 1
SubLabel.ZIndex           = 6
SubLabel.Parent           = LogoFrame

local GlowLine = Instance.new("Frame")
GlowLine.Size                   = UDim2.fromOffset(0, 1.5)
GlowLine.Position               = UDim2.fromOffset(0, 80)
GlowLine.AnchorPoint            = Vector2.new(0, 0)
GlowLine.BackgroundColor3       = Color3.fromRGB(200, 200, 200)
GlowLine.BackgroundTransparency = 0
GlowLine.BorderSizePixel        = 0
GlowLine.ZIndex                 = 6
GlowLine.Parent                 = LogoFrame
Instance.new("UICorner", GlowLine).CornerRadius = UDim.new(0, 1)

local ButtonsHolder = Instance.new("Frame")
ButtonsHolder.Size                   = UDim2.fromOffset(440, 60)
ButtonsHolder.Position               = UDim2.new(0.5, -220, 1, -130)
ButtonsHolder.BackgroundTransparency = 1
ButtonsHolder.ZIndex                 = 5
ButtonsHolder.Parent                 = MainScreen

local btnLayout = Instance.new("UIListLayout")
btnLayout.FillDirection       = Enum.FillDirection.Horizontal
btnLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
btnLayout.VerticalAlignment   = Enum.VerticalAlignment.Center
btnLayout.Padding             = UDim.new(0, 30)
btnLayout.Parent              = ButtonsHolder

local function MakeButton(text)
    local btn = Instance.new("TextButton")
    btn.Text                   = text
    btn.Font                   = Enum.Font.SourceSansBold
    btn.TextSize               = 18
    btn.TextColor3             = Color3.fromRGB(255, 255, 255)
    btn.BackgroundColor3       = Color3.fromRGB(18, 18, 18)
    btn.BackgroundTransparency = 0.15
    btn.AutoButtonColor        = false
    btn.Size                   = UDim2.fromOffset(190, 52)
    btn.TextTransparency       = 1
    btn.ZIndex                 = 6
    btn.Parent                 = ButtonsHolder
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 12)

    AddRotatingBorder(btn, 1.4, 100)

    btn.MouseButton1Down:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.12, Enum.EasingStyle.Quad), {
            BackgroundColor3       = Color3.fromRGB(40, 40, 40),
            BackgroundTransparency = 0,
        }):Play()
    end)
    btn.MouseButton1Up:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.18, Enum.EasingStyle.Quad), {
            BackgroundColor3       = Color3.fromRGB(18, 18, 18),
            BackgroundTransparency = 0.15,
        }):Play()
    end)
    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), { BackgroundTransparency = 0.05 }):Play()
    end)
    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {
            BackgroundColor3 = Color3.fromRGB(18, 18, 18),
            BackgroundTransparency = 0.15
        }):Play()
    end)
    return btn
end

local PlayBtn = MakeButton("تشغيل")
local InfoBtn = MakeButton("حقوقنا")

local function MakeBackBtn(parent, zidx)
    local btn = Instance.new("TextButton")
    btn.Text                   = "رجوع ←"
    btn.Font                   = Enum.Font.SourceSansBold
    btn.TextSize               = 14
    btn.TextColor3             = Color3.fromRGB(220, 220, 220)
    btn.BackgroundColor3       = Color3.fromRGB(15, 15, 15)
    btn.BackgroundTransparency = 0.2
    btn.AutoButtonColor        = false
    btn.Size                   = UDim2.fromOffset(110, 36)
    btn.ZIndex                 = zidx or 10
    btn.Parent                 = parent
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 9)
    AddRotatingBorder(btn, 1.2, 80)

    btn.MouseButton1Down:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.1), {
            BackgroundColor3       = Color3.fromRGB(40, 40, 40),
            BackgroundTransparency = 0,
        }):Play()
    end)
    btn.MouseButton1Up:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.15), {
            BackgroundColor3       = Color3.fromRGB(15, 15, 15),
            BackgroundTransparency = 0.2,
        }):Play()
    end)
    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.15), { BackgroundTransparency = 0.08 }):Play()
    end)
    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.15), {
            BackgroundColor3 = Color3.fromRGB(15, 15, 15),
            BackgroundTransparency = 0.2
        }):Play()
    end)
    return btn
end

local InfoScreen = Instance.new("Frame")
InfoScreen.Size                   = UDim2.new(1, 0, 1, 0)
InfoScreen.BackgroundTransparency = 1
InfoScreen.Visible                = false
InfoScreen.ZIndex                 = 7
InfoScreen.Parent                 = BlackBG

local InfoPanel = Instance.new("Frame")
InfoPanel.Size                   = UDim2.fromOffset(420, 340)
InfoPanel.Position               = UDim2.new(1, -440, 0, 60)
InfoPanel.BackgroundColor3       = Color3.fromRGB(8, 8, 8)
InfoPanel.BackgroundTransparency = 0.04
InfoPanel.BorderSizePixel        = 0
InfoPanel.ZIndex                 = 8
InfoPanel.Parent                 = InfoScreen
Instance.new("UICorner", InfoPanel).CornerRadius = UDim.new(0, 18)
AddRotatingBorder(InfoPanel, 1.6, 70)

local InfoPanelHeader = Instance.new("Frame")
InfoPanelHeader.Size                   = UDim2.new(1, -24, 0, 48)
InfoPanelHeader.Position               = UDim2.fromOffset(12, 12)
InfoPanelHeader.BackgroundTransparency = 1
InfoPanelHeader.ZIndex                 = 9
InfoPanelHeader.Parent                 = InfoPanel
do
    local hl = Instance.new("UIListLayout", InfoPanelHeader)
    hl.FillDirection       = Enum.FillDirection.Horizontal
    hl.VerticalAlignment   = Enum.VerticalAlignment.Center
    hl.HorizontalAlignment = Enum.HorizontalAlignment.Left
    hl.Padding             = UDim.new(0, 10)
end

local HeaderTitle = Instance.new("TextLabel")
HeaderTitle.Text                   = "VXS HUB"
HeaderTitle.Font                   = Enum.Font.SourceSansBold
HeaderTitle.TextSize               = 26
HeaderTitle.TextColor3             = Color3.fromRGB(255, 255, 255)
HeaderTitle.BackgroundTransparency = 1
HeaderTitle.AutomaticSize          = Enum.AutomaticSize.X
HeaderTitle.Size                   = UDim2.new(0, 0, 1, 0)
HeaderTitle.TextXAlignment         = Enum.TextXAlignment.Left
HeaderTitle.ZIndex                 = 10
HeaderTitle.Parent                 = InfoPanelHeader

local headerGrad = Instance.new("UIGradient", HeaderTitle)
headerGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0,   Color3.fromRGB(255, 255, 255)),
    ColorSequenceKeypoint.new(0.6, Color3.fromRGB(180, 180, 180)),
    ColorSequenceKeypoint.new(1,   Color3.fromRGB(80,  80,  80)),
})
headerGrad.Rotation = 90

local InfoBackBtn = MakeBackBtn(InfoPanelHeader, 11)

local Divider = Instance.new("Frame")
Divider.Size                   = UDim2.new(1, -24, 0, 1)
Divider.Position               = UDim2.fromOffset(12, 66)
Divider.BackgroundColor3       = Color3.fromRGB(100, 100, 100)
Divider.BackgroundTransparency = 0.4
Divider.BorderSizePixel        = 0
Divider.ZIndex                 = 9
Divider.Parent                 = InfoPanel

local InfoScroll = Instance.new("ScrollingFrame")
InfoScroll.Size                    = UDim2.new(1, -24, 1, -84)
InfoScroll.Position                = UDim2.fromOffset(12, 76)
InfoScroll.BackgroundTransparency  = 1
InfoScroll.BorderSizePixel         = 0
InfoScroll.ScrollBarThickness      = 3
InfoScroll.ScrollBarImageColor3    = Color3.fromRGB(100, 100, 100)
InfoScroll.CanvasSize              = UDim2.new(0, 0, 0, 0)
InfoScroll.AutomaticCanvasSize     = Enum.AutomaticSize.Y
InfoScroll.ScrollingDirection      = Enum.ScrollingDirection.Y
InfoScroll.ZIndex                  = 9
InfoScroll.Parent                  = InfoPanel

local InfoContent = Instance.new("Frame")
InfoContent.Size                   = UDim2.new(1, 0, 0, 0)
InfoContent.AutomaticSize          = Enum.AutomaticSize.Y
InfoContent.BackgroundTransparency = 1
InfoContent.ZIndex                 = 9
InfoContent.Parent                 = InfoScroll

local contentLayout = Instance.new("UIListLayout")
contentLayout.Padding   = UDim.new(0, 8)
contentLayout.SortOrder = Enum.SortOrder.LayoutOrder
contentLayout.Parent    = InfoContent

Instance.new("UIPadding", InfoContent).PaddingBottom = UDim.new(0, 14)

local function AddInfoRow(_, label, value)
    local row = Instance.new("Frame")
    row.Size                   = UDim2.new(1, 0, 0, 46)
    row.BackgroundColor3       = Color3.fromRGB(18, 18, 18)
    row.BackgroundTransparency = 0.3
    row.BorderSizePixel        = 0
    row.ZIndex                 = 10
    row.Parent                 = InfoContent
    Instance.new("UICorner", row).CornerRadius = UDim.new(0, 10)
    AddRotatingBorder(row, 1, 60)

    local lbl = Instance.new("TextLabel")
    lbl.Text                   = label
    lbl.Font                   = Enum.Font.SourceSansBold
    lbl.TextSize               = 11
    lbl.TextColor3             = Color3.fromRGB(140, 140, 140)
    lbl.BackgroundTransparency = 1
    lbl.Size                   = UDim2.new(0.5, 0, 1, 0)
    lbl.Position               = UDim2.fromOffset(14, 0)
    lbl.TextXAlignment         = Enum.TextXAlignment.Left
    lbl.ZIndex                 = 11
    lbl.Parent                 = row

    local val = Instance.new("TextLabel")
    val.Text                   = value
    val.Font                   = Enum.Font.SourceSansBold
    val.TextSize               = 12
    val.TextColor3             = Color3.fromRGB(255, 255, 255)
    val.BackgroundTransparency = 1
    val.Size                   = UDim2.new(0.5, -14, 1, 0)
    val.Position               = UDim2.new(0.5, 0, 0, 0)
    val.TextXAlignment         = Enum.TextXAlignment.Right
    val.ZIndex                 = 11
    val.Parent                 = row
end

AddInfoRow("", "الاسم",    "VxS Hub")
AddInfoRow("", "الإصدار",  "v0.1")
AddInfoRow("", "اللعبة",   "Brookhaven RP")
AddInfoRow("", "الحماية",  "Anti-Spy نشط")

local devTitleFrame = Instance.new("Frame")
devTitleFrame.Size                   = UDim2.new(1, 0, 0, 28)
devTitleFrame.BackgroundTransparency = 1
devTitleFrame.ZIndex                 = 10
devTitleFrame.Parent                 = InfoContent

local devTitleLbl = Instance.new("TextLabel")
devTitleLbl.Text                   = "المطورين"
devTitleLbl.Font                   = Enum.Font.SourceSansBold
devTitleLbl.TextSize               = 13
devTitleLbl.TextColor3             = Color3.fromRGB(160, 160, 160)
devTitleLbl.BackgroundTransparency = 1
devTitleLbl.Size                   = UDim2.new(1, 0, 1, 0)
devTitleLbl.Position               = UDim2.fromOffset(4, 0)
devTitleLbl.TextXAlignment         = Enum.TextXAlignment.Left
devTitleLbl.ZIndex                 = 11
devTitleLbl.Parent                 = devTitleFrame

local DevSection = Instance.new("Frame")
DevSection.Size                   = UDim2.new(1, 0, 0, 0)
DevSection.AutomaticSize          = Enum.AutomaticSize.Y
DevSection.BackgroundTransparency = 1
DevSection.ZIndex                 = 10
DevSection.Parent                 = InfoContent

local DevLayout = Instance.new("UIListLayout")
DevLayout.Padding   = UDim.new(0, 7)
DevLayout.SortOrder = Enum.SortOrder.LayoutOrder
DevLayout.Parent    = DevSection

local devNames = {"سكاي", "فيلموريث"}
for _, name in ipairs(devNames) do
    local row = Instance.new("Frame")
    row.Size                   = UDim2.new(1, 0, 0, 44)
    row.BackgroundColor3       = Color3.fromRGB(18, 18, 18)
    row.BackgroundTransparency = 0.3
    row.BorderSizePixel        = 0
    row.ZIndex                 = 10
    row.Parent                 = DevSection
    Instance.new("UICorner", row).CornerRadius = UDim.new(0, 10)
    AddRotatingBorder(row, 1, 55)

    local dot = Instance.new("Frame")
    dot.Size             = UDim2.fromOffset(7, 7)
    dot.Position         = UDim2.new(0, 14, 0.5, -3)
    dot.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    dot.BorderSizePixel  = 0
    dot.ZIndex           = 11
    dot.Parent           = row
    Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)

    local label = Instance.new("TextLabel")
    label.Text                   = name
    label.Font                   = Enum.Font.SourceSansBold
    label.TextSize               = 14
    label.TextColor3             = Color3.fromRGB(240, 240, 240)
    label.BackgroundTransparency = 1
    label.Size                   = UDim2.new(1, -40, 1, 0)
    label.Position               = UDim2.fromOffset(32, 0)
    label.TextXAlignment         = Enum.TextXAlignment.Left
    label.ZIndex                 = 11
    label.Parent                 = row
end

local function AntiSpy()
    local player = Players.LocalPlayer
    local PlayerGui = player:WaitForChild("PlayerGui")

    local blacklistedGUIs = {
        ["SimpleSpy2"] = true, ["SimpleSpy1"] = true, ["SimpleSpy"] = true,
        ["Spy"] = true, ["Simple_Spy"] = true, ["Simple-Spy"] = true,
        ["RemoteSpy"] = true, ["Remote_Spy"] = true, ["Remote-Spy"] = true,
        ["RemotesSpy"] = true, ["Remotes_Spy"] = true, ["Remotes-Spy"] = true,
        ["Remotes"] = true, ["Remote"] = true,
    }
    for i = 3, 20 do
        blacklistedGUIs["SimpleSpy"..i] = true
        blacklistedGUIs["Spy"..i]       = true
    end

    local function checkGui(child)
        if blacklistedGUIs[child.Name] then
            Players.LocalPlayer:Kick("Anti-Spy: Removed spy GUI")
        end
    end

    for _, gui in pairs(CoreGui:GetChildren())                    do checkGui(gui) end
    for _, gui in pairs(PlayerGui:GetChildren())                  do checkGui(gui) end
    CoreGui.ChildAdded:Connect(checkGui)
    PlayerGui.ChildAdded:Connect(checkGui)
end

local function SetupNameAndBio()
    task.spawn(function()
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local RE       = ReplicatedStorage:WaitForChild("RE")
        local nameText = RE:WaitForChild("1RPNam1eTex1t")
        local nameColor= RE:WaitForChild("1RPNam1eColo1r")
        nameText:FireServer("RolePlayName",  "جاري تحميل VxS")
        nameColor:FireServer("PickingRPNameColor", Color3.fromRGB(255, 255, 255))
        nameText:FireServer("RolePlayBio",   "بحث السكربت : VxS")
        nameColor:FireServer("PickingRPBioColor",  Color3.fromRGB(180, 180, 180))
    end)
end

local function LoadScriptAndExit(url, scriptType)
    SafePlaySound("rbxassetid://72989805559135", 0.6)
    AntiSpy()
    SetupNameAndBio()

    MainScreen.Visible = false
    InfoScreen.Visible = false

    StarImage.ZIndex = 100
    StarImage.Size   = UDim2.fromOffset(155, 155)

    TweenService:Create(StarImage, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size = UDim2.fromOffset(210, 210)
    }):Play()

    task.delay(0.3, function()
        SafePlaySound("rbxassetid://72989805559135", 0.7)
    end)

    task.delay(1.3, function()
        TweenService:Create(StarImage, TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
            ImageTransparency = 1,
            Size = UDim2.fromOffset(320, 320)
        }):Play()

        TweenService:Create(BlurEffect, TweenInfo.new(0.8, Enum.EasingStyle.Quad), { Size = 40 }):Play()

        task.delay(0.55, function()
            pcall(function() BlurEffect:Destroy() end)
            pcall(function() ScreenGui:Destroy()  end)

            task.spawn(function()
                if scriptType == "encrypt" then
                    loadstring(game:HttpGet(url))("9012111915890")
                else
                    loadstring(game:HttpGet(url))()
                end
            end)
        end)
    end)
end

local function ShowInfoScreen()
    MainScreen.Visible = false
    InfoScreen.Visible = true
    InfoPanel.Position = UDim2.new(1, -410, 0, 60)
    TweenService:Create(InfoPanel,
        TweenInfo.new(0.38, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        { Position = UDim2.new(1, -440, 0, 60) }
    ):Play()
end

local function HideInfoScreen()
    TweenService:Create(InfoPanel, TweenInfo.new(0.22, Enum.EasingStyle.Quad),
        { Position = UDim2.new(1, -410, 0, 60) }):Play()
    task.delay(0.25, function()
        InfoScreen.Visible = false
        MainScreen.Visible = true
    end)
end

InfoBackBtn.Activated:Connect(HideInfoScreen)
InfoBtn.Activated:Connect(ShowInfoScreen)

PlayBtn.Activated:Connect(function()
    LoadScriptAndExit("https://encrypt-x.pages.dev/Scripts?Id=9012111915890", "encrypt")
end)

StarImage.ImageTransparency        = 1
BgImageLabel.ImageTransparency     = 1
DarkOverlay.BackgroundTransparency = 1

TweenService:Create(BgImageLabel, TweenInfo.new(1.2), { ImageTransparency = 0.65 }):Play()
TweenService:Create(DarkOverlay,  TweenInfo.new(0.8), { BackgroundTransparency = 0.7 }):Play()
TweenService:Create(StarImage,    TweenInfo.new(1.4), { ImageTransparency = 0.0  }):Play()

LogoFrame.Position = UDim2.new(0.5, -320, -0.2, 0)
TweenService:Create(LogoFrame,
    TweenInfo.new(0.9, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
    { Position = UDim2.new(0.5, -320, 0.08, 0) }
):Play()

task.delay(0.4,  function() TweenService:Create(LogoTitle, TweenInfo.new(0.85), { TextTransparency = 0 }):Play() end)
task.delay(0.65, function()
    TweenService:Create(SubLabel, TweenInfo.new(0.65), { TextTransparency = 0 }):Play()
    TweenService:Create(GlowLine, TweenInfo.new(0.7),  { Size = UDim2.fromOffset(440, 1.5) }):Play()
end)
task.delay(0.9,  function()
    TweenService:Create(PlayBtn, TweenInfo.new(0.55, Enum.EasingStyle.Back), {
        BackgroundTransparency = 0.15,
        TextTransparency = 0
    }):Play()
end)
task.delay(1.1,  function()
    TweenService:Create(InfoBtn, TweenInfo.new(0.55, Enum.EasingStyle.Back), {
        BackgroundTransparency = 0.15,
        TextTransparency = 0
    }):Play()
end)

-- by Skay